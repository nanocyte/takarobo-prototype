# MASTER_TASK_LIST.md

## Project Overview
This project is a React Native chat application UI built with Expo, following the specifications outlined in `spec.md`. It features a Contacts screen and a Chat screen with mock data.

## Current Milestone Goals
- Implement the basic UI for Contacts and Chat screens according to `spec.md`.
- Set up navigation between the two screens using Expo Router.

## Major Features
- **Contacts Screen**: `contacts_screen_tasks.md` (Initial implementation done)
- **Chat Screen**: `chat_screen_tasks.md` (Initial implementation done)
- **Navigation**: `navigation_tasks.md` (Initial setup done)

## Completed Tasks (Archived)
- [x] Initialize Expo project.
- [x] Define UI spec in `spec.md`.
- [x] Create mock data structure (`src/data/mockData.ts`).
- [x] Implement `ContactsScreen` UI (`app/index.tsx`).
- [x] Implement `ChatScreen` UI (`app/chat/[id].tsx`).
- [x] Configure Stack Navigation using Expo Router (`app/_layout.tsx`).

---
*This file is maintained by AI and tracks the development progress.* 